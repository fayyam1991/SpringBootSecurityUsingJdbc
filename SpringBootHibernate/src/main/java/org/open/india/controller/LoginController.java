package org.open.india.controller;

public class LoginController {
	/*
	 * @Autowired private DefaultAuthenticationSerrvice loginServie;
	 * 
	 * @RequestMapping("/") public String user() { return "login"; }
	 * 
	 * @RequestMapping("loginUser") private String verifyUser(User user) { if
	 * (loginServie.isValidUser(user)) return "employee"; return "login"; }
	 */
}
